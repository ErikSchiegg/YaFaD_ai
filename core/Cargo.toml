[package]
name = "yafad_core"
version = "0.9.0"
edition = "2021"

[lib]
name = "yafad_core"
crate-type = ["staticlib", "cdylib"] # cdylib creates the .so file

[dependencies]
# Hier kommen später Mathe-Bibliotheken rein
